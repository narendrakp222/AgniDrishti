# Minimal runtime for the Flask web service (app.py).
# Only what the app imports at startup is listed here, so the
# build stays fast and reliable on Render / Docker / Railway.
# ML, GIS, and training libraries live in requirements.txt and are
# only needed by offline data-processing scripts.
Flask==3.1.3
gunicorn==26.2.0
numpy==2.5.3
pandas==3.0.5
